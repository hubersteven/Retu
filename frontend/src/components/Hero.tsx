import "../styles/hero.css";
import HeroCards from "./UI/HeroCards";

function Hero() {
  return (
    <section id="hero">
      <div id="hero-container">
        <HeroCards />
      </div>
    </section>
  );
}

export default Hero;
